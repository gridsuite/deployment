apiVersion: v1
kind: Service
metadata:
  name: {{ include "gridmerge.fullname" . }}
  labels:
    {{- include "gridmerge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gridmerge.selectorLabels" . | nindent 4 }}
