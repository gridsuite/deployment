apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gridmerge.fullname" . }}-test-connection"
  labels:
    {{- include "gridmerge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gridmerge.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
