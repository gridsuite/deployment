apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gridstudy.fullname" . }}-test-connection"
  labels:
    {{- include "gridstudy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gridstudy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
