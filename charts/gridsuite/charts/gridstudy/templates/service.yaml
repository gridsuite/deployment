apiVersion: v1
kind: Service
metadata:
  name: {{ include "gridstudy.fullname" . }}
  labels:
    {{- include "gridstudy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gridstudy.selectorLabels" . | nindent 4 }}
