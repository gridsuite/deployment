apiVersion: apps/v1
kind: Deployment
metadata:
  name: not-important
  annotations:
    gridsuite.org/cpu: cpu-xlarge
spec:
  template:
    spec:
      containers:
        - name: main
          resources:
            requests:
              cpu: "2500m"
            limits:
              cpu: "5000m"