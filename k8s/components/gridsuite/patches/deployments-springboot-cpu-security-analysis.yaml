apiVersion: apps/v1
kind: Deployment
metadata:
  name: not-important
  annotations:
    gridsuite.org/cpu: cpu-security-analysis
spec:
  template:
    spec:
      containers:
        - name: main
          resources:
            requests:
              cpu: "16000m"
            limits:
              cpu: "16000m"