apiVersion: batch/v1
kind: CronJob
metadata:
  name: not-important
spec:
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: main
            #TODO environment variables for secrets
