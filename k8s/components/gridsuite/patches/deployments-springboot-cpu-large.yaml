apiVersion: apps/v1
kind: Deployment
metadata:
  name: not-important
  annotations:
    gridsuite.org/cpu: cpu-large
spec:
  template:
    spec:
      containers:
        - name: main
          resources:
            requests:
              cpu: "2000m"
            limits:
              cpu: "4000m"