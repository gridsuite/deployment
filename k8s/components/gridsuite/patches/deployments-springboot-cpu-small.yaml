apiVersion: apps/v1
kind: Deployment
metadata:
  name: not-important
  annotations:
    gridsuite.org/cpu: cpu-small
spec:
  template:
    spec:
      containers:
        - name: main
          resources:
            requests:
              cpu: "500m"
            limits:
              cpu: "1000m"