apiVersion: apps/v1
kind: Deployment
metadata:
  name: not-important
spec:
  template:
    spec:
      volumes:
        - name: gridapps-env-configmap-volume
          configMap:
            name: gridapps-env-configmap
