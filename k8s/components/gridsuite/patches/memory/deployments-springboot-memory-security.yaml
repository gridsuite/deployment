apiVersion: apps/v1
kind: Deployment
metadata:
  name: not-important
  annotations:
    gridsuite.org/memory: memory-security
spec:
  template:
    spec:
      containers:
        - name: main
          env:
          - name: JAVA_TOOL_OPTIONS
            value: "-Xmx5760m"
          resources:
            requests:
              memory: "7680Mi"
            limits:
              memory: "7680Mi"
