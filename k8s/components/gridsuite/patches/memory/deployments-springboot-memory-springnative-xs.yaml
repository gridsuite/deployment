apiVersion: apps/v1
kind: Deployment
metadata:
  name: not-important
spec:
  template:
    spec:
      containers:
        - name: main
          resources:
            requests:
              memory: "256Mi"
            limits:
              memory: "256Mi"
