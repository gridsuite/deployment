apiVersion: apps/v1
kind: Deployment
metadata:
  name: not-important
  annotations:
    gridsuite.org/memory: memory-xxs
spec:
  template:
    spec:
      containers:
        - name: main
          env:
          - name: JAVA_TOOL_OPTIONS
            value: "-Xmx98m"
          resources:
            requests:
              memory: "130Mi"
            limits:
              memory: "130Mi"
