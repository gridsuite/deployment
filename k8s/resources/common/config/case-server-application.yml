spring:
  application:
    aws:
      bucket: bucket-gridsuite
      credentials:
        access-key: ${S3_ACCESS_KEY:minioadmin}
        secret-key: ${S3_SECRET_KEY:minioadmin}

storage:
  type: S3  # FS or S3
