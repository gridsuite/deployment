apiVersion: v1
kind: Service
metadata:
  labels:
    name: config-server
    app-prometheus-metrics: enabled
    app.kubernetes.io/component: gridsuite-springboot
  name: config-server
spec:
  selector:
    name: config-server
