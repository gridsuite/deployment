apiVersion: v1
kind: Service
metadata:
  labels:
    name: dynamic-simulation-server
    app.kubernetes.io/component: gridsuite-springboot
  name: dynamic-simulation-server
spec:
  selector:
    name: dynamic-simulation-server
