apiVersion: v1
kind: Service
metadata:
  labels:
    name: odre-server
    app-prometheus-metrics: enabled
    app.kubernetes.io/component: gridsuite-springboot
  name: odre-server
spec:
  selector:
    name: odre-server
