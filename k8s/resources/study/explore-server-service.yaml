apiVersion: v1
kind: Service
metadata:
  labels:
    name: explore-server
    app-prometheus-metrics: enabled
    app.kubernetes.io/component: gridsuite-springboot
  name: explore-server
spec:
  selector:
    name: explore-server
