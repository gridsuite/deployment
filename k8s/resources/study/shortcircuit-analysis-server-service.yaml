apiVersion: v1
kind: Service
metadata:
  labels:
    name: shortcircuit-analysis-server
    app.kubernetes.io/component: gridsuite-springboot
  name: shortcircuit-analysis-server
spec:
  selector:
    name: shortcircuit-analysis-server

