apiVersion: v1
kind: Service
metadata:
  labels:
    name: study-server
    app-prometheus-metrics: enabled
    app.kubernetes.io/component: gridsuite-springboot
  name: study-server
spec:
  selector:
    name: study-server
