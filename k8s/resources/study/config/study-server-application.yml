loadflow:
  default-provider: OpenLoadFlow

security-analysis:
  default-provider: OpenLoadFlow

sensitivity-analysis:
  default-provider: OpenLoadFlow

non-evacuated-energy:
  default-provider: OpenLoadFlow
