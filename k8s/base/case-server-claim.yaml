apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: gridsuite
    name: case-server-claim
  name: case-server-claim
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1000Mi
