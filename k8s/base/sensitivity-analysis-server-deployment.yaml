apiVersion: apps/v1
kind: Deployment
metadata:
  name: sensitivity-analysis-server
  labels:
    name: sensitivity-analysis-server
    version: "1"
spec:
  selector:
    matchLabels:
      name: sensitivity-analysis-server
  template:
    metadata:
      labels:
        name: sensitivity-analysis-server
    spec:
      containers:
        - name: sensitivity-analysis-server
          image: docker.io/gridsuite/sensitivity-analysis-server:latest
          imagePullPolicy: IfNotPresent
          env:
          - name: JAVA_TOOL_OPTIONS
            value: "-XX:MaxRAMPercentage=50"
          ports:
            - containerPort: 80
          volumeMounts:
            - mountPath: /config
              name: sensitivity-analysis-server-configmap-volume
            - mountPath: /config/common
              name: sensitivity-analysis-server-configmap-common-volume
            - mountPath: /config/specific
              name: sensitivity-analysis-server-configmap-specific-volume
            - mountPath: /home/powsybl/.itools
              name: sensitivity-analysis-server-itools-configmap-volume
            - mountPath: /tmp
              name: tmp-emptydir
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
          resources:
            requests:
              memory: "1.5Gi"
            limits:
              memory: "1.5Gi"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 120
            periodSeconds: 20
            timeoutSeconds: 2
            failureThreshold: 1
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 2
            failureThreshold: 1
            successThreshold: 1
          args: ["--spring.config.additional-location=/config/"]
      restartPolicy: Always
      volumes:
        - name: sensitivity-analysis-server-configmap-volume
          configMap:
            name: sensitivity-analysis-server-configmap
        - name: sensitivity-analysis-server-configmap-common-volume
          configMap:
            name: sensitivity-analysis-server-configmap-common
        - name: sensitivity-analysis-server-configmap-specific-volume
          configMap:
            name: sensitivity-analysis-server-configmap-specific
        - name: sensitivity-analysis-server-itools-configmap-volume
          configMap:
            name: sensitivity-analysis-server-itools-configmap
        - name: tmp-emptydir
          emptyDir: {}
