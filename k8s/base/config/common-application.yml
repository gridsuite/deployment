spring:
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 103MB

  data:
    elasticsearch:
      enabled: true

server:
  error:
    include-message: always

logging:
    level:
        com.zaxxer.hikari.HikariConfig: debug
