apiVersion: v1
kind: Service
metadata:
  labels:
    name: gridexplore-app
  name: gridexplore-app
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 80
  selector:
    name: gridexplore-app
