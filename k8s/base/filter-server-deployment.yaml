apiVersion: apps/v1
kind: Deployment
metadata:
  name: filter-server
  labels:
    name: filter-server
    version: "1"
spec:
  selector:
    matchLabels:
      name: filter-server
  template:
    metadata:
      labels:
        name: filter-server
    spec:
      containers:
        - name: filter-server
          image: docker.io/gridsuite/filter-server:latest
          imagePullPolicy: IfNotPresent
          env:
          - name: JAVA_TOOL_OPTIONS
            value: "-XX:MaxRAMPercentage=75"
          ports:
            - containerPort: 80
          volumeMounts:
            - mountPath: /config
              name: filter-server-configmap-volume
          resources:
            requests:
              memory: "1Gi"
            limits:
              memory: "1Gi"
      restartPolicy: Always
      volumes:
        - name: filter-server-configmap-volume
          configMap:
            name: filter-server-configmap
