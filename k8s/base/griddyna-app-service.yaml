apiVersion: v1
kind: Service
metadata:
  labels:
    name: griddyna-app
  name: griddyna-app
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
  selector:
    name: griddyna-app
