apiVersion: v1
kind: Service
metadata:
  labels:
    name: report-server
  name: report-server
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 80
  selector:
    name: report-server
