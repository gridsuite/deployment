apiVersion: v1
kind: Service
metadata:
  labels:
    name: directory-notification-server
  name: directory-notification-server
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
  selector:
    name: directory-notification-server
