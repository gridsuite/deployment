apiVersion: v1
kind: Service
metadata:
  labels:
    name: study-server
  name: study-server
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
  selector:
    name: study-server
