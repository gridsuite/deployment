apiVersion: v1
kind: Service
metadata:
  labels:
    name: griddata-app
  name: griddata-app
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 80
  selector:
    name: griddata-app
