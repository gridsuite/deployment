apiVersion: v1
kind: Service
metadata:
  labels:
    name: dynamic-mapping-server
  name: dynamic-mapping-server
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
  selector:
    name: dynamic-mapping-server
