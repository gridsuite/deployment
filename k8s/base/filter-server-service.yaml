apiVersion: v1
kind: Service
metadata:
  labels:
    name: filter-server
  name: filter-server
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
  selector:
    name: filter-server
