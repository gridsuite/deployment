apiVersion: v1
kind: Service
metadata:
  labels:
    name: filter-server
  name: filter-server
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 80
  selector:
    name: filter-server
