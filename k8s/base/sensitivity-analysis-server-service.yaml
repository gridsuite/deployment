apiVersion: v1
kind: Service
metadata:
  labels:
    name: sensitivity-analysis-server
  name: sensitivity-analysis-server
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
  selector:
    name: sensitivity-analysis-server
