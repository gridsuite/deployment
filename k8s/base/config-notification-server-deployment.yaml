apiVersion: apps/v1
kind: Deployment
metadata:
  name: config-notification-server
  labels:
    name: config-notification-server
    version: "1"
spec:
  selector:
    matchLabels:
      name: config-notification-server
  template:
    metadata:
      labels:
        name: config-notification-server
    spec:
      containers:
      - name: config-notification-server
        image: docker.io/gridsuite/config-notification-server:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        volumeMounts:
        - mountPath: /config/common
          name: config-notification-server-configmap-common-volume
        - mountPath: /config/specific
          name: config-notification-server-configmap-specific-volume
        - mountPath: /tmp
          name: tmp-emptydir
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1000
          readOnlyRootFilesystem: true
        resources:
          requests:
            memory: "384Mi"
          limits:
            memory: "384Mi"
        livenessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 20
          timeoutSeconds: 2
          failureThreshold: 1
          successThreshold: 1
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 20
          timeoutSeconds: 2
          failureThreshold: 1
          successThreshold: 1
        args: ["--spring.config.additional-location=/config/"]
      restartPolicy: Always
      volumes:
        - name: config-notification-server-configmap-common-volume
          configMap:
            name: config-notification-server-configmap-common
        - name: config-notification-server-configmap-specific-volume
          configMap:
            name: config-notification-server-configmap-specific
        - name: tmp-emptydir
          emptyDir: {}
