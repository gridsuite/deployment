apiVersion: v1
kind: Service
metadata:
  labels:
    name: apps-metadata-service
  name: apps-metadata-service
spec:
  type: ClusterIP
  ports:
    - name: apps-metadata-service
      port: 80
      targetPort: 80
  selector:
    name: apps-metadata-service