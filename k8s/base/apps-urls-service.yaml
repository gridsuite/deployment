apiVersion: v1
kind: Service
metadata:
  labels:
    name: apps-urls
  name: apps-urls
spec:
  type: ClusterIP
  ports:
    - name: apps-urls
      port: 80
      targetPort: 80
  selector:
    name: apps-urls