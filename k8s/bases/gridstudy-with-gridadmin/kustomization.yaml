apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

#
# We don't deploy GridAdmin in the Azure/demo environment,
#  so we have a seperate base for gridadmin for now.
#

resources:
  - ../../resources/common
  - ../../resources/study
  - ../../resources/gridadmin
